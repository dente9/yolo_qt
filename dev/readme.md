# 命令

```
uv sync
.\.venv\Scripts\activate
pip download -d ./packlage package
uv pip install --offline --no-index --find-links ./packlage -r requirement.txt
uv venv -p 3.10
PySide6
ultralytics
dill
```
